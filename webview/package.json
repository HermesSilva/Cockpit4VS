{
  "name": "tootega-cockpit-webview",
  "version": "1.0.0",
  "private": true,
  "description": "React UI for the Tootega Cockpit Visual Studio extension. Built into the VSIX.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "node esbuild.mjs --production",
    "dev": "node esbuild.mjs",
    "watch": "node esbuild.mjs --watch",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "highlight.js": "^11.9.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "esbuild": "^0.21.5",
    "typescript": "^5.4.5"
  }
}
